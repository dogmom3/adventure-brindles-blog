<nav class="navbar">

    <div class="welcome">
      <h1 class="appname has-background-success is-size-1 has-text-weight-bold">Adventure Brindles Blog</h1>
      <h2 class="has-text-black is-size-4 has-text-weight-bold">Serving up the best recs for fun places to explore!</h2>
    </div>

    <div class="login has-text-black is-size-5 has-text-weight-bold has-background-success">
      {{#if isloggedin}}
     You are logged in as <div class="has-text-white is-size-5 has-text-weight-bold">{{user.firstName}} {{user.lastName}}</div>
      <a href="/logout"> <button type="btn" class="button is-black has-text-white has-text-weight-bold is-size-6">Logout</button></a>
      {{else}}
      {{>modal/login-block}}
      {{/if}}
      <h5 id="login-err-msg"></h5>
    </div>

</nav>